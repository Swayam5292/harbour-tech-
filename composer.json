{
    "name": "harbour-tech/root",
    "description": "Root proxy for Laravel on Vercel",
    "require": {
        "php": ">=8.1"
    },
    "scripts": {
        "vercel": [
            "cd harbour-manager && chmod -R 777 bootstrap/cache storage && composer install --no-dev --optimize-autoloader --no-interaction"
        ],
        "post-install-cmd": [
            "cd harbour-manager && composer install --no-dev --optimize-autoloader"
        ]
    }
}
